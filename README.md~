🚀 ShortBeyond API — Test Automation

Este repositório contém a automação de testes da ShortBeyond API, uma API RESTful de encurtamento de URLs desenvolvida em Go e utilizada no Projeto TestBeyond.

O objetivo é demonstrar boas práticas em automação de testes de API, organização de código, reutilização de componentes e facilidade de manutenção em ambientes reais.

📌 Sobre a API

A ShortBeyond API permite:

Cadastro e autenticação de usuários

Criação e gerenciamento de links encurtados

Redirecionamento por código curto

🔒 API exclusiva para alunos dos cursos do Projeto TestBeyond
Feito com ☕ e 💛 pelo Papito

🛠️ Tecnologias Utilizadas

Node.js — ambiente de execução

Playwright — test runner e assertions

TypeScript — tipagem e organização

Bruno — testes manuais e exploração da API

Podman — execução da API em containers

PostgreSQL — base de dados

Mermaid — diagrama de arquitetura

🚀 Instalação e Configuração
✅ Pré-requisitos

Node.js 18+

Podman / Podman Compose

Bruno CLI

📥 Clonando o repositório
git clone https://github.com/seu-usuario/shortbeyond-api-tests.git
cd shortbeyond-api-tests

📦 Instalando dependências
npm install

🐳 Subindo a API com Podman
podman-compose up -d


A API ficará disponível em:

http://localhost:3333/api

▶️ Executando os testes automatizados
npx playwright test


Modo interativo:

npx playwright test --ui

📂 Estrutura do Projeto
.
├── tests/
│   ├── auth/
│   ├── links/
│   └── redirect/
│
├── factories/
│   ├── user.factory.ts
│   └── link.factory.ts
│
├── fixtures/
│   └── api.fixture.ts
│
├── services/
│   ├── auth.service.ts
│   └── links.service.ts
│
├── database/
│   └── cleanup.ts
│
├── playwright.config.ts
└── README.md

⭐ Destaques da Arquitetura de Testes
🧪 Tests (E2E)

Testes organizados por domínio da API

Validação de fluxos completos

Cobertura de cenários positivos e negativos

🏭 Factories

Geração centralizada de dados de teste

Massa de dados previsível e reutilizável

Redução de duplicação nos testes

🧩 Fixtures

Setup e teardown centralizados

Gerenciamento de autenticação e contexto

Testes mais limpos e consistentes

🔌 Services

Camada responsável por chamadas HTTP

Encapsula regras de comunicação com a API

Evita lógica repetida nos testes

🗄 Database

Limpeza e preparação de dados de teste

Execução segura antes dos testes

Maior confiabilidade e isolamento

🧪 Exemplo de Teste Automatizado
import { test, expect } from '@playwright/test';
import { createUser } from '../factories/user.factory';

test('should create a user successfully', async ({ request }) => {
  const user = createUser();

  const response = await request.post('/auth/register', {
    data: user,
  });

  expect(response.status()).toBe(201);
});

📐 Diagrama de Arquitetura
graph TD
    Tests[Testes Automatizados]
    Fixtures[Fixtures]
    Factories[Factories]
    Services[Services]
    API[ShortBeyond API]
    DB[(Database)]

    Tests --> Fixtures
    Tests --> Factories
    Tests --> Services
    Services --> API
    API --> DB

🔍 Endpoints Cobertos
🔐 Autenticação

POST /auth/register

POST /auth/login

🔗 Links

POST /links

GET /links

DELETE /links/{id}

🔁 Redirecionamento

GET /{code}

📄 Documentação da API
/docs/api.yaml


Disponível via Swagger UI.

✂️ Versão Resumida

Automação de testes de API com Playwright

Arquitetura limpa e escalável

Uso de fixtures, factories, services e database

Ambiente controlado via Podman

README pronto para onboarding técnico

🤝 Contribuição

Projeto educacional. Sugestões e melhorias são bem-vindas via pull request.

Feito com ☕ e 💛 para o Projeto TestBeyond