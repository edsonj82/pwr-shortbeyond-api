# 🚀 ShortBeyond API — Test Automation

Este repositório contém a **automação de testes da ShortBeyond API**, uma API RESTful de encurtamento de URLs desenvolvida em **Go** e utilizada no **Projeto TestBeyond**.

O objetivo deste projeto é demonstrar **boas práticas em automação de testes de API**, organização de código, reutilização de componentes e facilidade de manutenção em ambientes reais.

---

## 📌 Sobre a API

A **ShortBeyond API** permite:

- Cadastro e autenticação de usuários  
- Criação e gerenciamento de links encurtados  
- Redirecionamento por código curto  

> 🔒 API exclusiva para alunos dos cursos do **Projeto TestBeyond**  
> Feito com ☕ e 💛 pelo Papito

---

## 🛠️ Tecnologias Utilizadas

- **Node.js**
- **Playwright**
- **TypeScript**
- **Bruno**
- **Podman / Podman Compose**
- **PostgreSQL**
- **Mermaid**

---

## 🚀 Instalação e Configuração

### Pré-requisitos

- Node.js 18+
- Podman
- Bruno CLI

### Clonando o repositório

```bash
git clone https://github.com/edsonj82/pwr-shortbeyond-api.git
cd pwr-shortbeyond-api
```

### Instalando dependências

```bash
npm install
```

### Subindo a API com Podman

```bash
podman-compose up -d
```

A API ficará disponível em:

```
http://localhost:3333/api
```

### Executando os testes

```bash
npx playwright test
```

```bash
npx playwright test --ui
```

---

## 📂 Estrutura do Projeto

```
playwright/
├── e2e/
├── fixtures/
├── factories/
├── services/
└── support/
```

---

## ⭐ Destaques da Arquitetura de Testes

- **E2E**: testes por domínio e fluxo completo  
- **Factories**: geração de dados reutilizáveis  
- **Fixtures**: setup e teardown centralizados  
- **Services**: encapsulamento das chamadas HTTP  
- **Database**: limpeza e isolamento de dados  

---

## 🧪 Exemplo de Teste

```js
test('should create a user successfully', async ({ authorization }) => {
  const user = getUserWithLinks();
  const response = await authorization.createUser(user);
  expect(response.status()).toBe(201);
});
```

---

## 📐 Diagrama de Arquitetura

```mermaid
graph TD
    Tests --> Fixtures
    Tests --> Factories
    Tests --> Services
    Services --> API
    API --> DB
```

---

## 📄 Documentação da API

Arquivo OpenAPI:

```
/docs/api.yaml
```

---

## ✂️ Resumo

Projeto focado em **automação de testes de API profissional**, com arquitetura limpa, reutilizável e escalável.

---

Feito com ☕ e 💛 para o **Projeto TestBeyond**
